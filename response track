#include<stdio.h>
#include<stdbool.h>
int main()
{
    bool is_true=true;
    bool is_false=false;
    while(true)
    {
        int x;
        scanf("%d",&x);
        char y=(x>10)?'g':'l';
        printf("%c\n",y);
    }
}
